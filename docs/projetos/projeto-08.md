# Projeto 08 - Manipulador de Identidade 🌳

**Objetivo**: Praticar a seleção e a alteração de atributos e conteúdos do DOM.

## O Desafio
Crie um cartão de perfil simples em HTML.

1.  O cartão deve ter uma imagem (vazia ou placeholder), um nome (`h2`) e uma profissão (`p`).
2.  Crie um botão "Mudar Perfil".
3.  Quando o botão for clicado, o JavaScript deve:
    - Selecionar o `h2` e mudar o nome para "Ricardo Prates".
    - Selecionar o `p` e mudar a profissão para "Desenvolvedor Full Stack".
    - Selecionar a imagem e mudar o `src` para uma URL de avatar real.

## O que entregar?
- O arquivo `.js` com as seleções e alterações.
- O arquivo `.html` simples com os elementos e o ID correto para seleção.
- Um print do "antes e depois" do cartão de perfil.