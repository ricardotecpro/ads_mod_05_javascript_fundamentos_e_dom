# Exercícios 01 - Introdução ao JavaScript 😊

## 🟢 Básicos (Fixação)

1.  **Conceito**: Explique a principal função do JavaScript no desenvolvimento de uma página web e como ele se diferencia do HTML e CSS.
2.  **Sintaxe**: Escreva o código necessário para exibir a mensagem "Olá, Mundo!" no console do navegador e também em um alerta na tela.

## 🟡 Intermediários (Aplicação)

3.  **Organização**: Crie uma estrutura de pastas simples com `index.html` e `main.js`. Mostre como você faria a ligação entre esses dois arquivos para que o script seja executado após o carregamento do HTML.
4.  **Ferramentas**: Abra o console do seu navegador (F12) e execute três comandos diferentes de `console` (log, warn, error). Descreva o que muda visualmente entre eles no console.

## 🔴 Desafio

5.  **Mini Automação**:
    Utilizando apenas o console do navegador, declare uma variável com seu nome e outra com sua idade. Em seguida, exiba uma mensagem única no console que diga: "Meu nome é [seu nome] e eu tenho [sua idade] anos."
    *Dica: Você pode usar o operador `+` para juntar os textos.*
