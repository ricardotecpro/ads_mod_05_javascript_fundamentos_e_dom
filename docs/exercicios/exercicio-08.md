# Exercícios 08 - Introdução ao DOM 🌐

## 🟢 Básicos (Fixação)

1.  **Seleção**: Qual comando você usaria para selecionar um elemento `<h1 id="titulo">`?
2.  **Conteúdo**: Como você mudaria o texto de um parágrafo para "Novo conteúdo"?

## 🟡 Intermediários (Aplicação)

3.  **Seletores CSS**: Selecione o primeiro item de uma lista que possui a classe `item-ativo` e mude sua cor para azul.
4.  **Diferença**: Explique por que usar `textContent` é geralmente mais seguro do que `innerHTML`.

## 🔴 Desafio

5.  **Relógio Simples (Conceitual)**:
    - Imagine um `<span>` com o ID `relogio`.
    - Como você faria para, via JavaScript, atualizar o texto desse span com o valor da hora atual toda vez que um botão fosse clicado?
    - Dica: use `new Date().toLocaleTimeString()`.